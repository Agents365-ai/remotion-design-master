/**
 * Hooks Index
 */

export { useTheme, useThemeColors, useThemeTypography, useThemeSpacing, ThemeProvider, default as useThemeDefault } from './useTheme'
export {
  useProgress,
  useFade,
  useSlide,
  useScale,
  useLoop,
  usePulse,
  useStaggerDelay,
  useSectionProgress,
  default as useAnimationDefault,
} from './useAnimation'
export {
  useVideoProgress,
  useCurrentTime,
  useFormattedTime,
  useInRange,
  useRangeProgress,
  useChapterProgress,
  default as useProgressDefault,
} from './useProgress'
